package com.mobiapps.recipekeeper.ui.dashboard

import androidx.lifecycle.ViewModel

// No @HiltViewModel in Phase 0 — no injected dependencies yet.
// Phase 1 will add @HiltViewModel and RecipeRepository injection.
class DashboardViewModel : ViewModel()
